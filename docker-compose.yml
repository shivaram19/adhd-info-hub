services:
  adhd:
    build: .
    container_name: adhd-app
    ports:
      - "8085:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certbot-www:/var/www/certbot
      - /etc/letsencrypt:/etc/letsencrypt:ro
